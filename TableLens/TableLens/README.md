# TableLens 代码结构

这个目录是 macOS App 的主源码目录。

- `App/`：应用入口与生命周期
- `Core/`：共享常量、模型、错误类型、展示辅助函数
- `Services/`：OCR、通知、加密存储等服务层
- `Hotkeys/`：全局热键注册与录制
- `Screenshot/`：截图选区与屏幕采集流程
- `Settings/`：设置窗口与输入组件
- `Info.plist`：应用元数据
